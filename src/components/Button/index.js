import React from 'react';
import Button from '@material-ui/core/Button';


const GenericButton = ({btnText}) => {
    return (
        <Button>{btnText}</Button>
    )
}

export default GenericButton
